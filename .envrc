# Force nix-direnv to treat the current flake as a plain path, avoiding libgit2 'git+file://' resolution
# which can fail with "object not found" on some setups. This copies the working tree to the store.
use flake path:.
